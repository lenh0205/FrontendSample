```js
// trong React, ví dụ trong 1 hàm async có 2 logic setState sau đó gọi Hàm
// sau khi setState, ta gọi 1 hàm sync thì nó sẽ chờ; nhưng nếu hàm ta gọi là async thì sao
// nếu ta gọi hàm đó mà có thêm await đằng trước thì sao
```